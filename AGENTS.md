# Personal OS execution rules

Read `PERSONAL_AI_CONSTITUTION.md` before modifying Personal OS behavior. Apply the rules for the relevant execution layer and preserve the user's current instructions and platform permission boundaries.

Google Tasks remains the source of truth for ordinary task content and completion. Calendar is a projection. Reuse the existing intake, lifecycle, schedule and audit paths.

Run the repository's existing `npm run verify` for behavioral changes. Local checks do not establish production deployment or iPhone acceptance; report those states separately.
