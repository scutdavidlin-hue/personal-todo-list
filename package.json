{
  "name": "personal-todo-list",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check today.js && node --check src/core.js && node --check src/cloud-client.js && node scripts/verify.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}

