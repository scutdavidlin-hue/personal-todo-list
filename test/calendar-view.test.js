import test from 'node:test';
import assert from 'node:assert/strict';
import {monthDays,eventOnDay,calendarItems,shanghaiTime} from '../src/calendar-view.js';
test('month grid includes all days and stable Sunday rows across year and leap month',()=>{for(const month of ['2026-09','2027-01','2028-02']){const days=monthDays(month);assert.equal(days.length,42);assert.equal(new Date(days[0]+'T00:00:00').getDay(),0);assert.equal(new Set(days).size,42);assert.ok(days.includes(month+'-28'));}});
test('all-day end is exclusive and timed events convert to Shanghai',()=>{const event={start:{date:'2026-09-16'},end:{date:'2026-09-18'}};assert.equal(eventOnDay(event,'2026-09-16'),true);assert.equal(eventOnDay(event,'2026-09-17'),true);assert.equal(eventOnDay(event,'2026-09-18'),false);assert.equal(shanghaiTime('2026-09-16T18:30:00Z'),'2026-09-17T02:30');});
test('projection and task appear once, unscheduled tasks still appear, completed tasks do not',()=>{const events=[{id:'e',summary:'Meeting',personal_os_projection:true,start:{date:'2026-09-16'},end:{date:'2026-09-17'}}];const tasks=[{id:'t',date:'2026-09-16',title:'Meeting'},{id:'u',date:'2026-09-16',title:'Call'},{id:'c',date:'2026-09-16',done:true}];assert.equal(calendarItems(events,tasks,[{google_task_id:'t',calendar_event_id:'e'}],'2026-09-16').length,2);});
test('cross-midnight event is visible on both days, but exact midnight end does not spill',()=>{const event={start:{dateTime:'2026-09-16T23:00:00+08:00'},end:{dateTime:'2026-09-17T01:00:00+08:00'}};assert.ok(eventOnDay(event,'2026-09-16'));assert.ok(eventOnDay(event,'2026-09-17'));event.end.dateTime='2026-09-17T00:00:00+08:00';assert.equal(eventOnDay(event,'2026-09-17'),false);});
